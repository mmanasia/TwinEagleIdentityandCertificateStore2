<% if notice %>
    <p id="notice"><%= notice %></p>
<% end %>


<h1>Member Groups</h1>

<div style="overflow-x:auto;">
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      text-align: center;
      padding: 8px;
    }

    tr:nth-child(even){background-color: #f2f2f2}
  </style>


  <table>
    <thead>
    <tr>
      <th>Network Username</th>
      <th>User Group Name</th>
      <th colspan="3"></th>
    </tr>
    </thead>


    <tbody>
    <% @member_groups.each do |member_group| %>
        <tr>
          <td><%= member_group.NetworkUser.NetworkUsername %></td>
          <td><%= member_group.UserGroup.UserGroupName %></td>
          <td><%= link_to 'Show', member_group %></td>
          <td><%= link_to 'Edit', edit_member_group_path(member_group) %></td>
          <td><%= link_to 'Destroy', member_group, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
    <% end %>
    </tbody>
  </table>

</div>

<br>
