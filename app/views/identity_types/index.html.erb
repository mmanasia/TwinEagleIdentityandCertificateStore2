<% if notice %>
    <p id="notice"><%= notice %></p>
<% end %>


<h1>Identity Types</h1>

<div style="overflow-x:auto;">
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      text-align: center;
      padding: 8px;
    }

    tr:nth-child(even){background-color: #f2f2f2}
  </style>

  <%= link_to 'Create New Identity Type', new_identity_type_path %>

  <table>
    <thead>
    <tr>
      <th>Identity Type Name</th>
      <th>Identity Type Description</th>
      <th>Created By</th>
      <th>Created Date</th>
      <th>Last Modified By</th>
      <th>Last Modified Date</th>
      <th>Is Deleted</th>
      <th colspan="3"></th>
    </tr>
    </thead>


    <tbody>
    <% @identity_types.each do |identity_type| %>
        <tr>
          <td><%= identity_type.IdentityTypeName %></td>
          <td><%= identity_type.IdentityTypeDescription %></td>
          <td><%= identity_type.CreatedBy %></td>
          <td><%= identity_type.CreatedDate %></td>
          <td><%= identity_type.LastModifiedBy %></td>
          <td><%= identity_type.LastModifiedDate %></td>
          <td><%= identity_type.IsDeleted %></td>
          <td><%= link_to 'View', identity_type %></td>
          <td><%= link_to 'Update', edit_identity_type_path(identity_type) %></td>
          <td><%= link_to 'Delete', identity_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>

<br>
