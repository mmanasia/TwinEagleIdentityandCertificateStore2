<% if notice %>
    <p id="notice"><%= notice %></p>
<% end %>


<h1>Source Links</h1>

<div style="overflow-x:auto;">
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      text-align: center;
      padding: 8px;
    }

    tr:nth-child(even){background-color: #f2f2f2}
  </style>

  <%= link_to 'Create New Source Link', new_source_link_path %>

  <table>
    <thead>
    <tr>
      <th>Hyperlink</th>
      <th colspan="3"></th>
    </tr>
    </thead>

    <tbody>
    <% @source_links.each do |source_link| %>
        <tr>
          <td><%= source_link.Hyperlink %></td>
          <td><%= link_to 'View', source_link %></td>
          <td><%= link_to 'Update', edit_source_link_path(source_link) %></td>
          <td><%= link_to 'Delete', source_link, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>

<br>
